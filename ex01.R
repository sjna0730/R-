2+3

(3+6)*10
2^2 #2의 2제곱

10%%3 # 나머지 연산자 몫:3, 나머지:1

#함수 사용
log(10)+5 #로그함수
sqrt(100) #제곱근
max(10, 4, 2, 6, 100, 2000) #최대값을 구함
min(10, 4, 2, 6, 100, 2000) #최소값을 구함

#변수(variable) - 얼마든지 변할 수 있는 값
a <- 10 # a 변수에 10을 저장함
# <- 의 단축기는 alt + -
b <- 20
c <- a+b
print(c) #print() 함수를 이용하여 값을 출력함

a <- "a"
a + b # 문자 + 숫자는 연산이 안됨

b <- "b"
a+b #문자+문자는 연산이 안된다.

c <- TRUE
#NULL은 아무값을 지니지 않는다.
d <- NULL
d
#NaN값을 출력한다.
sqrt(-10)

#N/A - 결측치, 누락된 값